['slug]`), the `read_file` tool fails with "File not found', 'errors because the brackets `[]` are interpreted as pattern syntax rather than literal directory names.\n\n**Symptom:**\n```\nread_file(path=', ['slug]/page.tsx'], ")\nError: File not found: ['slug]/page.tsx']\n```\n\nEven though the file exists at `./app/shop/[slug]/page.tsx` in the codebase.\n\n## Root Cause\n\nThe Hermes file tools accept path arguments that may include shell-style pattern characters. Unescaped brackets are parsed as glob patterns, which do not match actual bracketed directory names.\n\nThis is particularly evident in WSL environments where:\n- File paths are case-sensitive\n- The `/mnt/` structure may require precise path resolution\n- Next.js App Router uses `[slug]` convention for dynamic segments\n\n## Workaround\n\nUse the `terminal` tool to read files with bracketed paths:\n\n```bash\n# Direct read works because shell interprets literally\ncat", '.', 'app/shop/[slug]/page.tsx"\n\n# Or use shell escaping\ncat', '.', 'app/shop/[slug]/page.tsx"\n```\n\nOr use `terminal` with `head/tail` for preview:\n\n```bash\nhead -20', '.', 'app/shop/[slug]/page.tsx', 'Permanent Solution Pattern\n\nWhen investigating Next.js dynamic route code, always use terminal-based file reading for paths containing `[` or `]`:\n\n```python\n# INSTEAD OF:\nread_file(path=', ['slug]/page.tsx'], ')  # FAILS\n\n# USE:\nterminal(command=', "head -50 './app/shop/[slug]/page.tsx'", 'Related Files\n\n- `app/shop/[slug]/page.tsx` — product detail page\n- `app/blog/[slug]/page.tsx` — blog post page\n- Any Next.js `[param]` dynamic route segment\n\n## Detection\n\nIf `read_file` repeatedly fails on a path you know exists, especially one containing `[` or `]`, switch to terminal reading immediately.\n\n## Checklist\n\n- [ ] Path contains `[` or `]`? → use terminal\n- [ ] Working in WSL? → extra careful with path escaping\n- [ ] File appears in `search_files` results but `read_file` fails? → terminal workaround\n\n## References\n\n- Next.js Dynamic Routes: https://nextjs.org/docs/app/building-your-application/routing/dynamic-routes\n- Hermes File Tools: paths with special chars require shell escaping']