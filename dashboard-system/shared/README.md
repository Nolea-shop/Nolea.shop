# Beispiel-Shared-Data für den Server-Betrieb
# Dieses Verzeichnis wird auf dem Server mit LIVE-Daten befüllt
# Siehe deploy.sh → rsync von deinem lokalen System per Tailscale

# Diary
/diary.json
/diary-stats.json
/diary-goals.json

# Token Usage
/tokens/usage.json

# Tasks
/tasks.json

# Logs
/logs/*.log
