# Multi-Agent System SSH Config Template
# Kopiere relevante Host-Blöcke in ~/.ssh/config
# Sobald die Partner-IPs bekannt sind, ersetze die Platzhalter

Host suffix-jeff
    HostName 100.103.196.11
    User damia
    IdentityFile ~/.ssh/id_ed25519
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null

# === Julian's Server (NAME) — nach IP-Eintrag aktivieren ===
# Host name-server
#     HostName <IP_VON_NAMES_SERVER>
#     User <username>
#     IdentityFile ~/.ssh/id_ed25519
#     StrictHostKeyChecking no
#     UserKnownHostsFile /dev/null

# === Julian's PC (ClaudiCloudy) — nach IP-Eintrag aktivieren ===
# Host cloudy-julian
#     HostName <IP_VON_CLAUDI_CLOUDY>
#     User <username>
#     IdentityFile ~/.ssh/id_ed25519
#     StrictHostKeyChecking no
#     UserKnownHostsFile /dev/null

# === Anleitung für Julian: Public Key installieren ===
# Auf dem Zielgerät (Server / Julian's PC) ausführen:
#   mkdir -p ~/.ssh && chmod 700 ~/.ssh
#   echo "<PUBKEY_INHALT>" >> ~/.ssh/authorized_keys
#   chmod 600 ~/.ssh/authorized_keys
# Public Key von Suffix/Jeff:
# SHA256:Nctq1+iyabIDEom3GDreTmLHTJ1ZRZST41D4gpV/6rs